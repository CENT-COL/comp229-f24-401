{
  "name": "comp299-f24-401",
  "version": "1.0.0",
  "description": "MERN Fullstack application",
  "main": "index.js",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "npx npm-run-all --parallel start:client start:server",
    "start:client": "npm --prefix client run dev",
    "start:server": "npm --prefix server run dev",
    "build": "npx npm-run-all --parallel build:client build:server",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server run build"
  },
  "author": "Thiago Castilho",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "antd": "^5.22.2",
    "bootstrap": "^5.3.3",
    "otplib": "^12.0.1",
    "qrcode": "^1.5.4",
    "websocket": "^1.0.35"
  }
}
